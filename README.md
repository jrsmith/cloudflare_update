A simple interface to update a Cloudflare-backed DNS record.

## Requirements

* a Cloudflare account
* Flask
* requests

## Notes

This is meant to be run behind your httpd's authentication mechanism. There's no
authentication built in to the app.
